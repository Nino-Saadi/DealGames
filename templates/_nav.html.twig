<nav class="navbar navbar-expand-lg navbar-light bg-primary">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <a href="{{ path('app_home') }}"><h1 class="text-white">Dealgames</h1></a>
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active ">
                <a class="nav-link text-light" href="{{ path('app_home') }}">Accueil</a>
            </li>
            {% if app.user %} 
            <li class="nav-item active">
                <a class="nav-link text-light" href="{{ path('app_product_index') }}">Produits</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link text-light" href="{{ path('app_product_new') }}">Ajouter un produits</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link text-light" href="{{ path('app_product_index') }}">Voir mes produits</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link text-light" href="">Connecté en tant que {{ app.user.name }}</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link text-light" href="{{ path('app_logout') }}">Se Déconnecter</i></a>
            </li>        
            {% else %}
            <li class="nav-item active ">
                <a class="nav-link text-light" href="{{ path('app_login') }}">Se Connecter</i></a>
            </li>
            <li class="nav-item active ">
                <a class="nav-link text-light" href="{{ path('app_register') }}">S'Inscrire</i></a>
            </li> 
            {% endif %}        
        </ul>
    </div>
</nav>